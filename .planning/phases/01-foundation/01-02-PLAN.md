---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/env.ts
  - src/env.test.ts
  - src/index.ts
  - .github/workflows/ci.yml
  - vitest.config.ts
autonomous: true
requirements:
  - ENV-01

must_haves:
  truths:
    - "detectEnv() returns 'bun' when running in Bun runtime"
    - "detectEnv() returns 'deno' when running in Deno runtime"
    - "detectEnv() returns 'node' when running in Node.js"
    - "detectEnv() returns 'browser' when running in a browser environment"
    - "SSR environments (global window absent) detect as 'node', not 'browser'"
    - "vitest tests pass for all environment detection branches"
    - "CI workflow runs build, lint, test, publint, and bundle size check"
  artifacts:
    - path: "src/env.ts"
      provides: "Runtime environment detection"
      exports: ["detectEnv", "RuntimeEnv"]
      min_lines: 20
    - path: "src/env.test.ts"
      provides: "Tests for environment detection"
      min_lines: 30
    - path: "vitest.config.ts"
      provides: "Vitest test configuration"
      contains: "defineConfig"
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI pipeline"
      contains: "publint"
  key_links:
    - from: "src/index.ts"
      to: "src/env.ts"
      via: "re-exports detectEnv and RuntimeEnv"
      pattern: "export.*from.*env"
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "runs pnpm build, test, lint, check-exports"
      pattern: "pnpm (build|test|lint|check-exports)"
---

<objective>
Implement the runtime environment detector and set up CI with vitest, publint validation, and bundle size gating.

Purpose: ENV-01 requires auto-detecting Node/Bun/Deno/Browser at runtime. CI ensures the exports map stays valid and the core bundle stays under 20KB gzipped across all future changes.
Output: Working environment detector with full test coverage, vitest configuration, and GitHub Actions CI workflow.
</objective>

<execution_context>
@/home/konstantina/.claude/get-shit-done/workflows/execute-plan.md
@/home/konstantina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement environment detector with tests</name>
  <files>src/env.ts, src/env.test.ts, vitest.config.ts</files>
  <behavior>
    - detectEnv() returns 'bun' when globalThis.Bun exists (Bun check first per priority)
    - detectEnv() returns 'deno' when globalThis.Deno exists and Bun does not
    - detectEnv() returns 'browser' when typeof window !== 'undefined' AND typeof document !== 'undefined' (both required to avoid SSR false positives)
    - detectEnv() returns 'node' when typeof process !== 'undefined' and process.versions?.node exists
    - detectEnv() returns 'node' as fallback for SSR environments (window exists but document does not, or process exists)
    - detectEnv() returns 'unknown' if no runtime matches (defensive, should not happen in practice)
    - Detection priority order: Bun -> Deno -> Node -> Browser -> unknown
    - RuntimeEnv type is 'node' | 'bun' | 'deno' | 'browser' | 'unknown'
  </behavior>
  <action>
1. Create vitest.config.ts: import { defineConfig } from 'vitest/config'. Set test.include to ['src/**/*.test.ts'], test.environment to 'node'.

2. Write src/env.test.ts FIRST (RED phase):
   - Import detectEnv from './env'
   - Test suite "detectEnv" with tests that mock globalThis properties:
     - "returns 'node' in default vitest environment" — no mocking needed, vitest runs in Node
     - "returns 'bun' when Bun global exists" — set globalThis.Bun = {} before, delete after
     - "returns 'deno' when Deno global exists" — set globalThis.Deno = { version: {} } before, delete after
     - "returns 'bun' over 'deno' when both exist" — set both globals, expect 'bun' (priority test)
     - "returns 'browser' when window and document exist" — mock globalThis.window and globalThis.document, remove process.versions temporarily
     - "returns 'node' for SSR (window exists but no document)" — mock window only with process.versions intact, expect 'node'
   - Use beforeEach/afterEach to save and restore original globals to prevent test pollution
   - Run tests: `pnpm test` — they should FAIL because src/env.ts does not exist yet

3. Write src/env.ts (GREEN phase):
   - Export type `RuntimeEnv = 'node' | 'bun' | 'deno' | 'browser' | 'unknown'`
   - Export function `detectEnv(): RuntimeEnv` implementing the priority chain:
     - Check `typeof globalThis.Bun !== 'undefined'` -> return 'bun'
     - Check `typeof globalThis.Deno !== 'undefined'` -> return 'deno'
     - Check `typeof process !== 'undefined' && process.versions?.node` -> return 'node'
     - Check `typeof window !== 'undefined' && typeof document !== 'undefined'` -> return 'browser'
     - Default: return 'unknown'
   - Use `declare global` block to type Bun and Deno on globalThis without importing their type packages
   - Run tests: `pnpm test` — all should PASS

4. Update src/index.ts: add `export { detectEnv, type RuntimeEnv } from './env'`
5. Run `pnpm build` to verify everything still compiles with the new module.
  </action>
  <verify>
    <automated>cd /home/konstantina/repos/omnifs && pnpm test 2>&1 | tail -20</automated>
  </verify>
  <done>All environment detection tests pass. detectEnv() correctly identifies Node (default), Bun (when global exists), Deno (when global exists), Browser (when window+document exist without process), and falls back to Node for SSR. The function and type are re-exported from src/index.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Set up GitHub Actions CI with publint and bundle size gate</name>
  <files>.github/workflows/ci.yml</files>
  <action>
1. Create .github/workflows/ci.yml with a workflow named "CI" triggered on push to main and all pull requests.
2. Single job "build-and-test" running on ubuntu-latest with steps:
   - Checkout code (actions/checkout@v4)
   - Setup Node 20 (actions/setup-node@v4 with node-version: 20)
   - Setup pnpm (pnpm/action-setup@v4 — it reads packageManager from package.json)
   - Install dependencies: `pnpm install --frozen-lockfile`
   - Lint: `pnpm lint`
   - Build: `pnpm build`
   - Test: `pnpm test`
   - Validate exports: `pnpm check-exports` (publint + attw)
   - Bundle size gate: Use a run step that checks gzipped size of dist/index.mjs:
     ```
     SIZE=$(gzip -c dist/index.mjs | wc -c)
     echo "Core bundle size: $SIZE bytes gzipped"
     if [ "$SIZE" -gt 20480 ]; then
       echo "FAIL: Core bundle exceeds 20KB gzipped ($SIZE bytes)"
       exit 1
     fi
     ```
3. Ensure the packageManager field is set in package.json (pnpm/action-setup reads it). Add "packageManager": "pnpm@9.15.4" (or current stable) to package.json if not already present. Run `pnpm -v` first to get the exact version installed.
  </action>
  <verify>
    <automated>cd /home/konstantina/repos/omnifs && cat .github/workflows/ci.yml | head -50 && pnpm build && SIZE=$(gzip -c dist/index.mjs | wc -c) && echo "Bundle size: ${SIZE} bytes" && test "$SIZE" -lt 20480 && echo "PASS: Under 20KB"</automated>
  </verify>
  <done>CI workflow file exists at .github/workflows/ci.yml with all required steps (lint, build, test, publint, attw, bundle size gate). Bundle size check passes locally. packageManager field is set in package.json.</done>
</task>

</tasks>

<verification>
1. `pnpm test` passes with all environment detection tests green
2. `pnpm build` succeeds
3. `pnpm check-exports` passes
4. `.github/workflows/ci.yml` exists and contains steps for lint, build, test, publint, attw, and bundle size
5. `gzip -c dist/index.mjs | wc -c` reports under 20480 bytes
6. `node -e "const { detectEnv } = require('./dist/index.cjs'); console.log(detectEnv())"` prints "node"
</verification>

<success_criteria>
- Environment detector correctly identifies Node runtime (verified by test)
- All detection branches covered by unit tests (Bun, Deno, Browser, Node, SSR)
- Detection priority is Bun > Deno > Browser > Node (verified by priority test)
- CI workflow validates exports, types, tests, lint, and bundle size on every push
- Bundle size gate enforces the 20KB gzipped limit for the core bundle
- detectEnv and RuntimeEnv are exported from the main entry point
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
